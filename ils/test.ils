s: bool = true;
limit: int = 1;

fun add(a: int, b: int) -> int
{
	return a + b;
}

fun main() -> int
{
	while(s == true)
	{ 
		if(limit > 100)
		{
			break;
		}
		@println(limit);
		limit = add(limit, limit);
	}
	return 0;
}