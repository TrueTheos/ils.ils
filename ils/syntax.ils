// Comment
/*
    Block comment
*/

@builtIn

i++;
i--;

&& || 

!= > < <= >= ==
& | ^ ! << >> >>>

number: int = 10;
letter: char = 'c';
word: str = "word";
boolean: bool = true; // false

array: [int] = {10,5};
arraySize: [int, 5]; // {0,0,0,0,0} 

fun power(x: int) -> int {
    /* ... */
}

fun print(msg: str)
{
    /* ... */
}

if(number > 0) { }
elif(number < 0) {}
else {}

while(number > 10)
{
    number = number - 1;
}

enum Status {
    planned,
    wip,
    finished,
    totest
}

for(array; value){}

for(array[0..1]; value){}

for(array[2..0]; value){}

currentStatus: Status = Status.planned;

// Exhaustive
switch(currentStatus)
{
    Status.planned:
    {

    }
    Status.wip: { }
    Status.totest, Status.finished:{}
    _: {}
}